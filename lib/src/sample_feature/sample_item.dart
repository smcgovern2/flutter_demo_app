/// A placeholder class that represents an entity or model.
class SampleItem {
  const SampleItem(this.id, this.name, this.imageName, this.route);
  final String imageName;
  final String name;
  final String route;
  final int id;
}
